Some information

- NPM init -y in the root folder because it makes it easier to deploy
- Type: Module in the package.json file to use import instead of require
- In the scripts write... Allows for running npm run dev instead of node ./backend.server.js
    - "dev": "node backend/server.js"
- npm install nodemon -D makes it a node dependency... Meaning it'll allow to see changes after running npm run dev


- Collection vs Tables
    - Collections
        - Collection of documents
    - Tables
        - Tables form and uses rows and columns




- Frontend
    - Installed react-router-dom to allow for multiple pages